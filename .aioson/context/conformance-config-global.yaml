# Conformance Contract — config-global
# Generated by: @analyst
# Verified by: @qa

feature: config-global
spec_version: 1
generated_at: 2026-04-12T10:00:00Z

acceptance_criteria:
  - id: AC-config-global-01
    description: "Visualização inicial de campos preenchidos ou vazios."
    type: behavior
    preconditions:
      - "Painel do AIOSON Play acessível"
    action: "Acessar a rota /auth/settings"
    expected:
      - "A interface exibe o formulário"
      - "Os dados populam o formulário via chamada de API GET"
    negative_cases: []

  - id: AC-config-global-02
    description: "Salvar credenciais de forma segura (Criptografia no BD)."
    type: security
    preconditions:
      - "Página de configurações aberta"
    action: "Salvar o formulário"
    expected:
      - "Dados de Client ID e Hosts são salvos em texto plano"
      - "Dados de Passwords e Secrets são armazenados cifrados na base"
    negative_cases: []

  - id: AC-config-global-03
    description: "Não retornar segredos descriptografados para a API do Frontend."
    type: security
    preconditions:
      - "Configurações já foram salvas"
    action: "Fazer GET na API de configurações"
    expected:
      - "Os campos sensíveis (google_client_secret, github_client_secret, smtp_pass) são omitidos ou retornados apenas como flags booleanas / mascarados"
    negative_cases: []

  - id: AC-config-global-04
    description: "Padrão Singleton."
    type: data
    preconditions:
      - "Registro já existe na base"
    action: "Alterar configuração e salvar"
    expected:
      - "O registro atual é atualizado e o número de registros na tabela continua sendo 1."
    negative_cases: []
